#include "ammarep.h"
#include "bayesreg.h"

int main(int argc,char *argv[])
{
  void expr_main(int argc,char *argv[]);
  memory_leak_check_args(argc,argv);
  printf("The extra library currently has no useful executable.\n");
  am_malloc_report();
  return 0;
}
